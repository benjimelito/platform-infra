name: Provision Lovable App (prod)

on:
  workflow_dispatch:
    inputs:
      target_repo:
        description: "Target repo in owner/name format (e.g. benjimelito/angular-demo)"
        required: true
        type: string

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "dispatch works for ${{ inputs.target_repo }}"